PORT=8080

# Project Root (Backend logic uses this base path)
# USE ABSOLUTE PATH!!!
PROJECT_ROOT=

# Frontend API URL (Vite requires VITE_ prefix)
VITE_API_URL=http://localhost:8080

# Custom Git Ignore (Comma separated list of filenames/patterns to ignore in AI context)
# Example: package-lock.json,pnpm-lock.yaml,*.map
GIT_IGNORE_FILE=package-lock.json,pnpm-lock.yaml