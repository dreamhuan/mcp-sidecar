# Server Port
PORT=8080

# Project Root (Backend logic uses this base path)
# 注意：请使用绝对路径，不要使用 ~
PROJECT_ROOT=/home/fkq/workspace/vibe/mcp-sidecar

# Frontend API URL (Vite requires VITE_ prefix)
VITE_API_URL=http://localhost:8080

